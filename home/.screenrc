vbell off
vbell_msg ""

autodetach on
termcapinfo xterm ti@:te@
defscrollback 25000

# Status bar
caption always "%{= kw}%-w%{= BW}%n %t%{-}%+w %-= @%H - %LD %d %LM - %c"

# 0 becomes 10
# and Esc becomes 0.
bind 0 select 10
bind  select 0
